export default function(delay, handler) {
  var tid, evt;

  function callback() {
    handler(evt);
    tid = evt = null;
  }

  return function(e) {
    evt = e;
    if (tid) clearTimeout(tid);
    tid = setTimeout(callback, delay);
  };
}



//////////////////
// WEBPACK FOOTER
// ./node_modules/vega-lite/node_modules/vega-util/src/debounce.js
// module id = 2366
// module chunks = 1