export * from 'fs'
export resolve from 'path'
import {a} from './envdefault3.js'

class X {
  prop = 2
}

agf()
async function* agf() {}

const a1 = {a: 1}
const b1 = {...a1}

new Date(1, )

f()
async function f() {
  const b = [Promise.resolve(1)]
  for await (let a of b) ;
}
