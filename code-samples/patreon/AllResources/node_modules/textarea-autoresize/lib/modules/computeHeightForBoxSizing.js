'use strict';

exports.__esModule = true;
exports.default = computeHeightForBoxSizing;

var _computeVerticalStyle = require('./computeVerticalStyle');

/**
 * Returns height from content height(without padding or borders)
 *
 * @param {number} contentHeight height of content without padding and borders
 * @param {{boxSizing: string, verticalPadding: number, verticalBorders: number}} style
 * @returns {number}
 */
function computeHeightForBoxSizing(contentHeight, style) {
  if (style.boxSizing === 'border-box') {
    var verticalPadding = (0, _computeVerticalStyle.computeVerticalPadding)(style);
    var verticalBorders = (0, _computeVerticalStyle.computeVerticalBorders)(style);

    return contentHeight + verticalPadding + verticalBorders;
  }

  return contentHeight;
}


//////////////////
// WEBPACK FOOTER
// ./node_modules/textarea-autoresize/lib/modules/computeHeightForBoxSizing.js
// module id = ./node_modules/textarea-autoresize/lib/modules/computeHeightForBoxSizing.js
// module chunks = commons