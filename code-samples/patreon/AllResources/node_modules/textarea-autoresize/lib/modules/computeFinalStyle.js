'use strict';

exports.__esModule = true;
exports.default = computeFinalStyle;
/**
 * Computes final style
 *
 * @param {number} height
 * @param {{min: number, max: number}} limits
 * @returns {{finalHeight: number, finalOverflowY: string}}
 */
function computeFinalStyle(height, _ref) {
  var min = _ref.min;
  var max = _ref.max;

  var finalStyle = {
    height: height,
    overflowY: 'hidden'
  };

  if (height > max) {
    finalStyle.height = max;

    // Need to set `overflow-y` to `auto`
    // to make all the content visible
    finalStyle.overflowY = 'auto';
  } else if (height < min) {
    finalStyle.height = min;
  }

  return finalStyle;
}


//////////////////
// WEBPACK FOOTER
// ./node_modules/textarea-autoresize/lib/modules/computeFinalStyle.js
// module id = ./node_modules/textarea-autoresize/lib/modules/computeFinalStyle.js
// module chunks = commons