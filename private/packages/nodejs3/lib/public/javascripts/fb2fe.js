// fb2fe.js

// inter-module sharing
if (typeof HRMODULE == 'undefined') HRMODULE = {}

if (typeof $ == 'undefined') alert ('jQuery is missing')
else $(function() {
	// we have jQuery and document ready
	
})