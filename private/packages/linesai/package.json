{
  "name": "linesai",
  "version": "0.0.1",
  "description": "Async line iterator for read streams",
  "main": "lib/linesai.js",
  "scripts": {
    "build": "../../node_modules/.bin/rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Harald Rudell <harald.rudell@gmail.com> (http://haraldrudell.com)",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "rollupconfig": "0.0.1"
  }
}
