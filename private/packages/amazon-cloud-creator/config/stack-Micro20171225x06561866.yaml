Description: >
  Amazon Cloud Creator Microservice template for CloudFront Stack
  To add a new function define the function role and the function itself following
  existing templates.

Parameters:

  LambdaVersion:
    Description: "Version md5 of the lambda function required to update existing stack"
    Type: String
    Default: "latest"

  S3BucketName:
    Description: "S3BucketName"
    Type: "String"

Resources:
  Micro20171225x06561866Role:
    Type: "AWS::IAM::Role"
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          -
            Effect: "Allow"
            Principal:
              Service:
                - "lambda.amazonaws.com"
            Action:
              - "sts:AssumeRole"
      Policies:
        -
          PolicyName: "Micro20171225x06561866Policy"
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
            - Effect: Allow
              Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
              Resource: "*"

  Micro20171225x06561866:
    Type: "AWS::Lambda::Function"
    Properties:
      Code:
        S3Bucket: !Ref S3BucketName
        S3Key: !Sub Micro20171225x06561866-${LambdaVersion}.zip
      Description: Lambda function that schedules updates
      Environment:
        Variables:
          NODE_ENV: production
      FunctionName: Micro20171225x06561866
      Handler: "index.handler"
      MemorySize: 128
      Role: !GetAtt Micro20171225x06561866Role.Arn
      Runtime: "nodejs4.3"
      Timeout: 100