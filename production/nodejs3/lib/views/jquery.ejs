<!--
	ejs supports escape sequences
	%= for text, escapes ampersand, less than to avoid inserting html markup
	%- allows for inserting markup
	% execute JavaScript.
		There is no print function, but assigning a variable and doing a %= works
-->
<h1><%= title %></h1>
<%- result %>